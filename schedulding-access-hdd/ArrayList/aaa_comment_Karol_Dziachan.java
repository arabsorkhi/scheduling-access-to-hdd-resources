package ArrayList;

public @interface aaa_comment_Karol_Dziachan {

    /*
    I decided to keep all data in the arraylist, which is why I dedicated a separate package to implement it
    (it's not worth looking at, because nothing goes beyond the classic implementation)
     */
}
